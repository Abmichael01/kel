
# Robot body (Arduino over USB). Off by default; turn on once the Arduino is
# wired and you can access the serial port. Servo pin matches the sketch.
KEL_BODY_ENABLED=false
KEL_BODY_PORT=
KEL_BODY_SERVO_PIN=9
